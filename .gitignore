# Secrets
.env

# Dossier aveugle du pipeline (jamais stagé/commité/poussé)
.vault

# Données locales du serveur (banques de questions)
data/

# Build & dépendances
node_modules/
dist/
audit/*.png
audit/**/*.png

# macOS
.DS_Store

# Tests
tests/.data
tests/.data-integration
tests/.data-unit
tests/results/
playwright-report/
test-results/

# Artefacts de travail hors dépôt (photos, archives, exports ponctuels)
*.zip
PHOTO-*.jpg
design-board-standalone.html

# Données du balayage des redites (outil ponctuel, hors suite)
tests/.data-balayage
