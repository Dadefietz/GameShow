# Secrets
.env

# Dossier aveugle du pipeline (jamais stagé/commité/poussé)
.vault

# Données locales du serveur (banques de questions)
data/

# Build & dépendances
node_modules/
dist/
audit/*.png
audit/**/*.png

# macOS
.DS_Store

# Tests
# UN SEUL MOTIF POUR TOUS LES DOSSIERS DE DONNÉES D'ESSAI. Ils étaient énumérés un
# par un, et un dossier nommé au fil d'un déboguage — `tests/.data-tel` — est entré
# dans un commit parce qu'il ne figurait pas dans la liste. Une énumération oublie ;
# un motif, non.
tests/.data*
tests/results/
playwright-report/
test-results/

# Artefacts de travail hors dépôt (photos, archives, exports ponctuels)
*.zip
PHOTO-*.jpg
design-board-standalone.html

